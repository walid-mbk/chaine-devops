pipeline {
    agent any


    stages {
stage('Checkout') {
            steps {  
                git branch: 'main', credentialsId: 'Jenkins_GITHUB', url: 'git@github.com:malaksouhaila/projet.git'
            }
        }

        /*stage('Build') {
            steps {
                sh 'go build'
            }
        }
        stage('Test') {
            steps {
                sh 'go test ./...'
                #tt
            }
        }
        stage('Deploy') {
            steps {
                // Ajoutez ici les étapes de déploiement de votre application Go
            }
        }*/
    }
}
